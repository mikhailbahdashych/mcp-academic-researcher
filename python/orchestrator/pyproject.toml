[project]
name = "orchestrator"
version = "0.1.0"
description = "FastAPI orchestrator for MCP Academic Researcher"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.128.0",
    "httpx>=0.28.1",
    "pydantic>=2.12.5",
    "uvicorn>=0.40.0",
]
